import { RouteSkeleton } from "@/components/RouteSkeleton";

export default function Loading() {
  return (
    <RouteSkeleton
      title="Loading dashboard"
      description="Preparing treasury and governance overview."
    />
  );
}
