import { RouteSkeleton } from "@/components/RouteSkeleton";

export default function Loading() {
  return (
    <RouteSkeleton
      title="Loading proposal"
      description="Preparing the proposal timeline, vote totals, and actions."
    />
  );
}
